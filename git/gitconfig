[alias]
   b = branch
   ci = commit -am.
   co = checkout
   st = status
   bd = !git for-each-ref --sort='-authordate:iso8601' --format=' %(authordate:relative)%09%(refname:short)' refs/heads | sed -e 's-refs/heads/--'
[core]
	autocrlf = false
	filemode = false
[branch]
	autosetuprebase = always
